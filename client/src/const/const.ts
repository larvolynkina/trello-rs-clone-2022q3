export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

export enum NameSpace {
  user = 'USER',
}

export enum APIRoute {
  login = '/login',
  signup = '/signup',
  me = '/me',
}
